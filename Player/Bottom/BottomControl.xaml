
<UserControl x:Class="Player.Bottom.BottomControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:Player.Helpers"
             FontFamily="Segoe UI">

    <!-- 播放器控制栏容器 -->
    <Border Style="{StaticResource PlayerControlsContainer}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions> 
            <!-- 控制按钮区域 -->
            <Grid Grid.Row="0" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="0.25*" MinWidth="60"/>
                    <ColumnDefinition Width="*" MinWidth="200"/>
                    <ColumnDefinition Width="auto" MinWidth="80"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- 播放/暂停按钮 -->
                <Button Grid.Column="0" 
                        x:Name="PlayPauseButton" 
                        ToolTip="播放/暂停"
                        Click="PlayPauseButton_Click"
                        Style="{StaticResource SpecialShapePlayButton}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Margin="0,0,8,0">
                    <helpers:CustomIcon Kind="Play"  Foreground="White"  />
                </Button>
                
                <!-- 静音按钮 -->
                <Button Grid.Column="1" 
                        x:Name="MuteButton" 
                        ToolTip="静音"
                        Click="MuteButton_Click"
                        Style="{StaticResource CircularIconButton}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                    <helpers:CustomIcon Kind="VolumeHigh" 
                                      Foreground="{DynamicResource PrimaryBrush}" 
                                      Width="18" 
                                      Height="18"/>
                </Button>
                
                <!-- 音量滑块 -->
                <Slider Grid.Column="2" 
                        x:Name="VolumeSlider" 
                        Minimum="0" 
                        Maximum="100" 
                        Value="80" 
                        Orientation="Horizontal" 
                        ToolTip="音量调节"
                        Style="{StaticResource PlayerSlider}"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center"
                        Margin="15,0"
                        ValueChanged="VolumeSlider_ValueChanged"/>
                
                <!-- 播放进度条 -->
                <Slider Grid.Column="3" 
                        x:Name="ProgressBar" 
                        Minimum="0" 
                        Maximum="100" 
                        Value="0"  
                        Orientation="Horizontal"
                        Style="{StaticResource PlayerSlider}"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center"
                        Margin="20,0"
                        ValueChanged="ProgressSlider_ValueChanged"
                        PreviewMouseDown="ProgressBar_PreviewMouseDown"
                        PreviewMouseUp="ProgressBar_PreviewMouseUp"
                        ToolTip="播放进度"/>
                
                <!-- 时间显示 -->
                <TextBlock Grid.Column="4" 
                          x:Name="TimeDisplay" 
                          Text="00:00 / 00:00" 
                          Style="{StaticResource TimeDisplayStyle}"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          ToolTip="播放时间"/>
                
                <!-- 播放倍速下拉列表 -->
                <ComboBox Grid.Column="5" 
                          x:Name="SpeedComboBox" 
                          ToolTip="播放速度"
                          Style="{StaticResource CompactComboBox}"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Margin="8,0,8,0"
                          SelectionChanged="SpeedComboBox_SelectionChanged">
                    <ComboBoxItem Content="0.5x"/>
                    <ComboBoxItem Content="1.0x" IsSelected="True"/>
                    <ComboBoxItem Content="1.5x"/>
                    <ComboBoxItem Content="2.0x"/>
                </ComboBox>
                
                <!-- 全屏按钮 -->
                <Button Grid.Column="6" 
                        x:Name="FullscreenButton" 
                        ToolTip="全屏"
                        Click="FullscreenButton_Click"
                        Style="{StaticResource CircularIconButton}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                    <helpers:CustomIcon Kind="Fullscreen" 
                                      Foreground="{DynamicResource PrimaryBrush}" 
                                      Width="20" 
                                      Height="20"/>
                </Button>
                
            </Grid>
        </Grid>
    </Border>
</UserControl>