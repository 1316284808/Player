<UserControl x:Class="Player.Left.LeftControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Player.Left" 
             xmlns:helpers="clr-namespace:Player.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="300">
    
    <!-- 移除本地资源定义，使用应用程序级别的主题资源 -->
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- 左侧边栏 -->
        <Grid Grid.Column="0" Width="50">
            <Border Background="{DynamicResource SecondaryBrush}" Width="50">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2"   Opacity="0.1"/>
                </Border.Effect>

                <StackPanel VerticalAlignment="Top" HorizontalAlignment="Center" Margin="5">
                    <!-- 折叠按钮 -->
                    <Button x:Name="CollapseButton" Margin="2" Style="{StaticResource PlayerIconButton}" ToolTip="折叠侧边栏" HorizontalAlignment="Center" Click="CollapseButton_Click">
                        <helpers:CustomIcon x:Name="CollapseIcon" Kind="ChevronLeft" Foreground="{DynamicResource PrimaryBrush}" Width="28" Height="28"/>
                    </Button>
                    
                    <!-- 设置按钮 -->
                    <Button x:Name="SettingsButton" Margin="2" Style="{StaticResource PlayerIconButton}" ToolTip="设置" HorizontalAlignment="Center" Click="SettingsButton_Click">
                        <helpers:CustomIcon Kind="Settings" Foreground="{DynamicResource PrimaryBrush}" Width="24" Height="24"/>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- 右侧内容区域 -->
        <Border CornerRadius="8" Grid.Column="1"
                Background="#FAFAFA"
                Opacity="0.95" 
                Margin="10"
                HorizontalAlignment="Stretch" 
                Width="220">
            <Border.Effect>
                <DropShadowEffect BlurRadius="8" ShadowDepth="2" Color="Black" Opacity="0.1"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="7*"/>
                    <RowDefinition Height="3*"/>
                </Grid.RowDefinitions>
                
                <!-- 播放列表区域 -->
                <Border Grid.Row="0" Style="{StaticResource PlayerControlsContainer}" Margin="5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="1"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- 播放列表标题 -->
                            <Grid>
                                <TextBlock Text="播放列表" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Center"
                                           MouseDown="PlaylistTitle_MouseDown"/>
                                <Button Margin="1" Style="{StaticResource PlayerIconButton}" Width="35" Height="35" ToolTip="添加文件夹" HorizontalAlignment="Right"
                                        Command="{Binding DataContext.OpenFolderCommand, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}">
                                    <helpers:CustomIcon Kind="FolderPlus" Foreground="{DynamicResource PrimaryBrush}" Width="18" Height="18"/>
                                </Button>
                            </Grid>
                        <!-- 分隔线 -->
                        <Border Grid.Row="1" Background="{DynamicResource SecondaryDarkBrush}" Height="1" Margin="0 0 0 0"/>

                        <!-- 播放列表 -->
                        <ListBox x:Name="PlaylistListBox" 
                                 Grid.Row="2" 
                                 ItemsSource="{Binding DataContext.Playlist, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" 
                                 SelectionChanged="PlaylistListBox_SelectionChanged" 
                                 Background="Transparent" 
                                 BorderThickness="0" 
                                 Margin="0"
                                 HorizontalAlignment="Stretch" 
                                 Width="Auto"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 HorizontalContentAlignment="Stretch">
                            <!-- 自定义滚动条样式 -->
                            <ListBox.Resources>
                                <!-- 垂直滚动条样式 -->
                                <Style TargetType="ScrollBar">
                                    <Setter Property="Width" Value="6"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Opacity" Value="0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ScrollBar">
                                                <Grid Background="Transparent">
                                                    <Track x:Name="PART_Track" IsDirectionReversed="True">
                                                        <Track.DecreaseRepeatButton>
                                                            <RepeatButton Command="ScrollBar.LineUpCommand" Opacity="0" Focusable="False"/>
                                                        </Track.DecreaseRepeatButton>
                                                        <Track.Thumb>
                                                            <Thumb>
                                                                <Thumb.Style>
                                                                    <Style TargetType="Thumb">
                                                                        <Setter Property="Background" Value="#B0B0B0"/>
                                                                        <Setter Property="Template">
                                                                            <Setter.Value>
                                                                                <ControlTemplate TargetType="Thumb">
                                                                                    <Border x:Name="ThumbBorder" 
                                                                                            Background="{TemplateBinding Background}" 
                                                                                            CornerRadius="3" 
                                                                                            Width="6"/>
                                                                                    <ControlTemplate.Triggers>
                                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                                            <Setter TargetName="ThumbBorder" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                                                                        </Trigger>
                                                                                        <Trigger Property="IsDragging" Value="True">
                                                                                            <Setter TargetName="ThumbBorder" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                                                                        </Trigger>
                                                                                    </ControlTemplate.Triggers>
                                                                                </ControlTemplate>
                                                                            </Setter.Value>
                                                                        </Setter>
                                                                    </Style>
                                                                </Thumb.Style>
                                                            </Thumb>
                                                        </Track.Thumb>
                                                        <Track.IncreaseRepeatButton>
                                                            <RepeatButton Command="ScrollBar.LineDownCommand" Opacity="0" Focusable="False"/>
                                                        </Track.IncreaseRepeatButton>
                                                    </Track>
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <!-- 鼠标悬停在ListBox上时显示滚动条 -->
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Opacity" Value="1"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                                <!-- ScrollViewer样式 -->
                                <Style TargetType="ScrollViewer">
                                    <Setter Property="Padding" Value="0 0 8 0"/>
                                </Style>
                            </ListBox.Resources>

                            <!-- 子项模板 -->
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" Tag="{Binding Path}" Padding="10" 
                                               TextTrimming="CharacterEllipsis" 
                                               TextWrapping="NoWrap" 
                                               Focusable="True"
                                               HorizontalAlignment="Stretch">
                                        <TextBlock.ToolTip>
                                            <ToolTip Content="{Binding Name}" />
                                        </TextBlock.ToolTip>
                                    </TextBlock>
                                </DataTemplate>
                            </ListBox.ItemTemplate>

                            <!-- ListBox模板 -->
                            <ListBox.Template>
                                <ControlTemplate TargetType="{x:Type ListBox}">
                                    <Border CornerRadius="0 0 8 8" Background="Transparent" BorderThickness="0">
                                        <ScrollViewer Padding="0" Focusable="false" 
                                                      VerticalScrollBarVisibility="Auto" 
                                                      HorizontalScrollBarVisibility="Disabled">
                                            <ItemsPresenter SnapsToDevicePixels="True"/>
                                        </ScrollViewer>
                                    </Border>
                                </ControlTemplate>
                            </ListBox.Template>

                            <!-- 子项容器样式 -->
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Margin" Value="2 1"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                                <Border x:Name="Bd" 
                                                        Background="{TemplateBinding Background}"
                                                        CornerRadius="6"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <!-- 选中状态 -->
                                                    <Trigger Property="IsSelected" Value="true">
                                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SecondaryBrush}"/>
                                                        <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                                        <Setter TargetName="Bd" Property="BorderThickness" Value="1"/>
                                                        <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
                                                    </Trigger>
                                                    <!-- 悬停状态 -->
                                                    <Trigger Property="IsMouseOver" Value="true">
                                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SecondaryLightBrush}"/>
                                                        <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                                        <Setter TargetName="Bd" Property="BorderThickness" Value="1"/>
                                                    </Trigger>
                                                    <!-- 禁用状态 -->
                                                    <Trigger Property="IsEnabled" Value="false">
                                                        <Setter TargetName="Bd" Property="Opacity" Value="0.6"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                    </Grid>
                </Border>
                
                <!-- 历史记录区域 -->
                <Border Grid.Row="1" Style="{StaticResource PlayerControlsContainer}" Margin="5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="1"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- 历史记录标题 --> 
                            <TextBlock Text="历史记录" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}"/>
                          
                        <!-- 分隔线 -->
                        <Border Grid.Row="1" Background="{DynamicResource SecondaryDarkBrush}" Height="1" Margin="0 0 0 0"/>
                        
                        <!-- 历史记录列表 -->
                        <ListBox x:Name="HistoryListBox"
                                 Grid.Row="2"
                                 SelectionChanged="HistoryListBox_SelectionChanged"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Margin="0"
                                 HorizontalAlignment="Stretch"
                                 Width="Auto"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 HorizontalContentAlignment="Stretch">
                            <!-- 自定义滚动条样式 -->
                            <ListBox.Resources>
                                <Style TargetType="ScrollBar">
                                    <Setter Property="Width" Value="6"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Opacity" Value="0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ScrollBar">
                                                <Grid Background="Transparent">
                                                    <Track x:Name="PART_Track" IsDirectionReversed="True">
                                                        <Track.DecreaseRepeatButton>
                                                            <RepeatButton Command="ScrollBar.LineUpCommand" Opacity="0" Focusable="False"/>
                                                        </Track.DecreaseRepeatButton>
                                                        <Track.Thumb>
                                                            <Thumb>
                                                                <Thumb.Style>
                                                                    <Style TargetType="Thumb">
                                                                        <Setter Property="Background" Value="#B0B0B0"/>
                                                                        <Setter Property="Template">
                                                                            <Setter.Value>
                                                                                <ControlTemplate TargetType="Thumb">
                                                                                    <Border x:Name="ThumbBorder"
                                                                                            Background="{TemplateBinding Background}"
                                                                                            CornerRadius="3"
                                                                                            Width="6"/>
                                                                                    <ControlTemplate.Triggers>
                                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                                            <Setter TargetName="ThumbBorder" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                                                                        </Trigger>
                                                                                        <Trigger Property="IsDragging" Value="True">
                                                                                            <Setter TargetName="ThumbBorder" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                                                                        </Trigger>
                                                                                    </ControlTemplate.Triggers>
                                                                                </ControlTemplate>
                                                                            </Setter.Value>
                                                                        </Setter>
                                                                    </Style>
                                                                </Thumb.Style>
                                                            </Thumb>
                                                        </Track.Thumb>
                                                        <Track.IncreaseRepeatButton>
                                                            <RepeatButton Command="ScrollBar.LineDownCommand" Opacity="0" Focusable="False"/>
                                                        </Track.IncreaseRepeatButton>
                                                    </Track>
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Opacity" Value="1"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.Resources>
                            
                            <!-- 子项模板 -->
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" Padding="10" 
                                               TextTrimming="CharacterEllipsis" 
                                               TextWrapping="NoWrap" 
                                               Focusable="True"
                                               HorizontalAlignment="Stretch">
                                        <TextBlock.ToolTip>
                                            <ToolTip Content="{Binding}" />
                                        </TextBlock.ToolTip>
                                    </TextBlock>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                            
                            <!-- 子项容器样式 -->
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Margin" Value="2 1"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                                <Border x:Name="Bd"
                                                        Background="{TemplateBinding Background}"
                                                        CornerRadius="6"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsSelected" Value="true">
                                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SecondaryBrush}"/>
                                                        <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                                        <Setter TargetName="Bd" Property="BorderThickness" Value="1"/>
                                                        <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="true">
                                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SecondaryLightBrush}"/>
                                                        <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                                        <Setter TargetName="Bd" Property="BorderThickness" Value="1"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>